<IfModule mod_rewrite.c>
    RewriteEngine On

    # Nếu request không phải là thư mục / file thì tự động chuyển vào /public
    RewriteCond %{REQUEST_URI} !^/public
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>